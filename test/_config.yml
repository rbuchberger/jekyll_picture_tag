# Base Jekyll configuration

name: Jane Doe
description: The personal website of Jane Doe

author:
  name: Jane Doe
  email: jane.doe@gmail.com
  github: jdoe
  twitter: jdoe

# Technical Configuration

exclude: ['_scss', 'image', 'css', 'js', 'fonts', 'bower_components', '**.png', '**.jpg', '**.jpeg', '**.gif', '**.webp', '**.svg', '**.ico']
include: ['.htaccess']
permalink: pretty

# Liquid Picturefill Configuration

liquidpicturefill:
  source_dir: "rawimages"
  output_dir: "assets/images"
  # Array off PPIs, a variation will be generated for each, don't go too crazy!
  ppi: [1, 1.3, 3]
  options:
    # If the tag is not given an argument it will use the following parameters
    default:
      attr:
        class: "blogimage"
      default:
        # Obviously one size parameter is necessary
        # one argument scales
        # two arguments scale and crop
        width: "500"
      media: "(min-width: 60em)"
        width: "750"
      media: "(min-width: 90em)"
        width: "1000"
    # Any second tier name would be a preset to be referenced
    gallery:
      attr:
        # Attributes are completely open ended, add whatever you like
        class: "blogimage"
        data-image: "gallery"
      default:
        width: "250"
        height: "150"
      media: "(min-width: 60em)"
        width: "375"
        height: "225"
      media: "(min-width: 90em)"
        width: "500"
        height: "300"