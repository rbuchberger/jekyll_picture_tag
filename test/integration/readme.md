# All inputs
- Tag initialization
  * tag name (ignored)
  * raw params
  * tokens (ignored)
- Render
  * context
- loads default.yml

## Context object queries
- `context.environments.first['jekyll']['environment']`
- `context.registers[:site].config`
- site url
- site baseurl
- 

# All outputs

